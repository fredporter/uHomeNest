# uHOME-server Getting Started

1. Review `docs/boundary.md`.
2. Review `docs/activation.md`.
3. Run `scripts/run-uhome-server-checks.sh`.
4. Keep service code under `services/` and `modules/`.
5. Route scheduling behavior through `scheduling/`.
6. Add tests before extending persistent behavior.
